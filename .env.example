# Admin panel hidden path (set by install.sh to random alphanumeric; change to your own if manual)
ADMIN_PATH=/manage-x7k9m2

# First main account (only used when DB has no admin users; set by install.sh if missing)
# INIT_ADMIN_USER=admin
# INIT_ADMIN_PASS=your-secure-password

# CORS: comma-separated origins; empty = allow all (e.g. same-origin / dev)
# ALLOWED_ORIGINS=https://yourdomain.com,https://admin.yourdomain.com

# Password hashing iterations (optional; default 300000)
# PBKDF2_ITERATIONS=300000

# Backend
PORT=3001
NODE_ENV=production
DB_PATH=/app/data/database.sqlite
